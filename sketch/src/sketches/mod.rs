use super::*;
pub(self) use nightsketch_derive::{sketch, sketchlist};

sketchlist! {
    blossom::Blossom,
    charcoal::Charcoal,
    manifold::Manifold,
    postcard::Postcard,
    weather::Weather,
}
